@model List<User>
<h2>All Users</h2><table class="table"><tr><th>Name</th><th>Email</th><th>Role</th><th>Accounts</th></tr>@foreach(var u in Model){<tr><td>@u.Name</td><td>@u.Email</td><td>@u.Role</td><td>@string.Join(", ",u.Accounts.Select(a=>a.AccountNumber))</td></tr>}</table>